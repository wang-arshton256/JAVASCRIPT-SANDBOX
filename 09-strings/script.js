let x;
const name = "Wangutusi";
const age = 30;
const address = "Bweyogerere";

//Template Literals
x = `My name is ${name},I am ${age} years old. I live at ${address}.` 
console.log(x);

//String Properties and methods
const s = 'Hello World';

x = typeof s; // To check what datatype variable (S) is of.
X = s.length // To check the length of a string.
X = s.toLowerCase();
X = s.toUpperCase();
X = s.trim();
X = s.split(); // To change string into an array.
x = s.indexOf(W) // Gives you the index of W
x = s.replace() // Replaces values in the string.

console.log(x);
